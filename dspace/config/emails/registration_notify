## Registration notification email
##
## Parameters: {0} The name of the DSpace instance
##             {1} The URL of the DSpace instance
##             {2} Name:
##             {3} Email:
##             {4} Registration Date:
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($emailTitle = "Novo cadastro no repositório")
#set($emailPreheader = "Um novo usuário foi cadastrado no repositório.")
#set($subject = "${config.get('dspace.name')}: Registration Notification")

Um novo usuário foi cadastrado em ${params[0]}, em ${params[1]}:

Nome:                   ${params[2]}
E-mail:                 ${params[3]}
Data:                   ${params[4]}

A equipe do ${config.get('dspace.name')}
