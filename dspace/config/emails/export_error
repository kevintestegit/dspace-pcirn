## Email sent to DSpace users when they successfully export an item or collection.
##
## Parameters: {0} the export error
##             {1} the URL to the feedback page
##
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($emailTitle = "Falha na exportação")
#set($emailPreheader = "A exportação solicitada não foi concluída.")
#set($subject = "${config.get('dspace.name')}: The item export you requested was not completed.")
A exportação do item solicitada não foi concluída pelo seguinte motivo:
 ${params[0]}

Para mais informações, entre em contato com o administrador do sistema:
 ${params[1]}

A equipe do ${config.get('dspace.name')}
