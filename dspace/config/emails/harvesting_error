## E-mail sent to designated address when a harvest process fails
##
## Parameters: {0} Collection id
##             {1} Date & time
##             {2} Status flag
##             {3} Exception message
##             {4} Exception stack trace
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($emailTitle = "Falha na coleta")
#set($emailPreheader = "A coleta de uma coleção encontrou um erro.")
#set($subject = "${config.get('dspace.name')}: Harvesting Error")
Falha na coleta da coleção ${params[0]}:

Data:       	${params[1]}
Indicador de status: 	${params[2]}

${params[3]}

Exceção:
${params[4]}

A equipe do ${config.get('dspace.name')}
