## Notification email sent when a request to review an item has been tentatively rejected by the service
## This means that a resubmission is needed following review.
##
## Parameters: {0} Service Name
##             {1} Item Name
##             {2} Service URL
##             {3} Item URL
##             {4} Submitter's Name
##             {5} Date of the received LDN notification
##
##
#set($emailTitle = "Revisões necessárias na submissão")
#set($emailPreheader = "Uma submissão precisa de revisões após a análise do serviço.")
#set($subject = "DSpace: The Service ${params[0]} indicates that revisions are needed following review of the Item ""${params[1]}""")

O serviço ${params[0]} recebeu uma notificação de recusa provisória para o item: ${params[1]}. São necessárias revisões. Para prosseguir, envie uma versão revisada quando estiver pronta.

Veja o relatório detalhado:
Item: ${params[1]}
URL do item: ${params[3]}
Enviado por: ${params[4]}

Nova atualização: REVISIONS ARE NEEDED FOLLOWING REVIEW

Pelo serviço: ${params[0]}
URL do serviço: ${params[2]}
Data: ${params[5]}

A equipe do ${config.get('dspace.name')}
